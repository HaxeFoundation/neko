open Ast;
open Printer;
open Bytecode;
open LexEngine;

try {
	var i = IO.read_file "Test.neko" false;
	var lex = Lexer.create();
	Lexer.input lex "Test.neko" i 0 0;
	var t = Lexer.token lex (*Lex.expr);
} catch {
	e -> Core.print ("Exception : " + string e + "\n")
}