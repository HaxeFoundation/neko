try {
	var ctx = Printer.create IO.stdout;	
	var p = Ast.null_pos;
	print Printer.print;
	Printer.print ctx (Ast.EConst(Ast.Builtin "string"),p);	
} catch {
	e -> print ("Exception : " + string e + "\n")
}
